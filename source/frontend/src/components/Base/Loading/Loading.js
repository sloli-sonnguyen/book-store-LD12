import ReactLoading from 'react-loading';
import styled from 'styled-components';

export const PrimaryLoading = styled(ReactLoading)`
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`;
